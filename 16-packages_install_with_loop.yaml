- name: installing packkages
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: package installation
    ansible.builtin.package:
      name: "{{item}}"
      state: latest
    loop:
    - nginx
    - mysql
    - zip